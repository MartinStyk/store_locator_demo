package com.example.storelocator.model.map

import androidx.annotation.Px
import com.example.storelocator.model.location.GeoLocation

data class MapPinData(val location: GeoLocation, val title: String)